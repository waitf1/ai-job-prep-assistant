请基于以下内容输出简历修改建议的合法 JSON。

## 简历内容

{resume_text}

## 岗位 JD 结构化分析结果

{jd_analysis}

## 匹配分析结果

{match_analysis}

## RAG 检索片段

{retrieved_context}

## JSON 输出格式

{{
  "direct_edits": ["当前可以直接改写的简历表达1", "当前可以直接改写的简历表达2"],
  "future_edits": ["完成补充项目后可新增的表达1", "完成补充项目后可新增的表达2"]
}}

## 输出限制

- `direct_edits` 只能基于当前已有事实改写。
- `future_edits` 必须明确属于“完成后可写”或“建议补做后再写”的内容。
- 只输出 JSON 对象本身。
